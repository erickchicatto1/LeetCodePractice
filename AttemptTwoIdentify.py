class Solution:
    def getLargestOutlier(self, nums: List[int]) -> int:
        frecuency = {}
        restante =0
        totalNumber = 0

        for i in range(len(nums)):
            totalNumber += nums[i]
            
            if nums[i] == :
                restante = total - nums[i]
                
        #To count the frecuency
        for num in nums:
            if num in nums:
                nums[num] += 1     
